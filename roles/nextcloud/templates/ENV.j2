BACKUP_DIR="{{ nextcloud_backup_dir }}"
NEXTCLOUD_DIR="{{ nextcloud_data_dump_dir }}"
DATA_DIR="$NEXTCLOUD_DIR/_data"
DB_NAME="{{ nextcloud_db_name }}"
DB_USER="{{ nextcloud_db_user }}"
DB_PASSWORD="{{ nextcloud_db_password }}"
DB_CONTAINER="{{ nextcloud_db_container_name }}"
NEXTCLOUD_CONTAINER="{{ nextcloud_container_name }}"


SFTP_DIR="{{ sftp_server_dir }}"
SFTP_USER="{{ sftp_server_user }}"
SFTP_HOST="{{ sftp_server_url }}"
SFTP_PORT="{{ sftp_port }}